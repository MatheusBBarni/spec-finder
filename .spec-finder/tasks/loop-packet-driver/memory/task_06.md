# Task Memory: task_06

## Objective Snapshot

- Document `spec-finder loop` beside `run` in help and README with lockstep tests.

## Important Decisions

- Help Loop mode and README "Continuous loop vs run" name the seven terminals, exits 0/1/2/130, dry-run non-mutation, and deferred V1 work.
- README avoids the substring `loop --multiple` so lockstep tests can forbid advertising that grammar.

## Learnings

## Files / Surfaces

- `src/cli.tsx`
- `README.md`
- `tests/cli.test.ts`

## Errors / Corrections

## Ready for Next Run

- Packet implementation is complete. Local checkpoints remain blocked by gitignore vs tracked `.spec-finder/tasks`.
